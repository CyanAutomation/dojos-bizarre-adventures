name = "dojos-bizarre-adventures"
main = "worker/index.ts"
compatibility_date = "2026-09-07"

# Configure these secrets before deploying:
#   npx wrangler secret put API_KEY
#   npx wrangler secret put MCP_ALLOWED_HOSTNAMES
# REST_ALLOWED_ORIGINS is an optional comma-separated allowlist of browser origins.
